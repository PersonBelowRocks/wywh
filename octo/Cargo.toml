[package]
name = "octo"
version = "0.1.0"
edition = "2021"

[dependencies]
bytemuck = { version = "1.16.3", features = ["derive"] }
derive-new = "0.6.0"
glam = "0.28.0"
simba = { version = "0.9.0", features = ["libm"] }
